% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scFPCDE_gene_curves.R
\name{scFPCDE_gene_curves}
\alias{scFPCDE_gene_curves}
\title{Plot Observed and Fitted Gene-Expression Curves}
\usage{
scFPCDE_gene_curves(
  tt,
  yt,
  yt_fit,
  cell_cluster,
  subset = 1:12,
  facet_genes = TRUE,
  nrow = 4,
  ncol = 3,
  legend_dot_size = 3,
  point_size = 0.45,
  point_alpha = 0.55,
  curve_linewidth = 0.9,
  curve_color = "black",
  cluster_colors = NULL,
  scales = "free_y",
  title = NULL
)
}
\arguments{
\item{tt}{Numeric pseudotime vector with one value per row of `yt`.}

\item{yt}{Numeric observed expression matrix with observations in rows and
genes in columns. When plotting a result from [scFPCDE_run()], apply the
same centering and scaling used in that call. To plot an analysis fitted
with `center = TRUE` on its original expression scale, add the original
gene means back to `yt_fit` and supply the uncentered observations here.}

\item{yt_fit}{Numeric matrix of fitted expression values with the same
dimensions and gene names as `yt`.}

\item{cell_cluster}{Vector of cluster assignments with one value per row of
`yt`.}

\item{subset}{Optional vector of gene names or indices to plot. Use `NULL` to
plot every gene.}

\item{facet_genes}{Logical; if `TRUE`, draw genes in separate facets.}

\item{nrow}{Optional number of facet rows.}

\item{ncol}{Optional number of facet columns.}

\item{legend_dot_size}{Positive numeric size for points in the cluster legend.}

\item{point_size}{Positive numeric size for observed-expression points.}

\item{point_alpha}{Numeric transparency for observed-expression points in
`[0, 1]`.}

\item{curve_linewidth}{Positive numeric width for fitted trajectories.}

\item{curve_color}{Color used for fitted trajectories.}

\item{cluster_colors}{Optional vector of colors for cell clusters. A named
vector is matched to cluster labels; an unnamed vector is used in cluster
level order.}

\item{scales}{Facet scale behavior passed to [ggplot2::facet_wrap()]. One of
`"free_y"`, `"fixed"`, `"free"`, or `"free_x"`.}

\item{title}{Optional plot title. Use `NULL` for a context-specific default.}
}
\value{
A [ggplot2::ggplot()] object.
}
\description{
Displays FPCA-fitted trajectories over observed expression values. Observed
cells are colored by cluster and fitted trajectories are drawn as cohesive
curves. Genes can be faceted or overlaid in one panel.
}
\details{
All rows of `tt`, `yt`, `yt_fit`, and `cell_cluster` must describe the same
observations in the same order. The function orders these inputs together by
pseudotime before plotting. Duplicate gene names are disambiguated in facet
labels with [make.unique()].
}
\examples{
\donttest{
data(scFPCDE_simdata)
cells <- seq(1, 1000, length.out = 100)
cells <- unique(round(cells))
yt <- scale(scFPCDE_simdata$yt[cells, 1:12], center = TRUE, scale = FALSE)
fit <- scFPCDE_fit_fpca(yt, scFPCDE_simdata$tt[cells], nbasis = 20)
scFPCDE_gene_curves(
  tt = scFPCDE_simdata$tt[cells],
  yt = yt,
  yt_fit = fit$xt_hat,
  cell_cluster = scFPCDE_simdata$clusters[cells],
  point_size = 0.5,
  point_alpha = 0.6
)
}
}
